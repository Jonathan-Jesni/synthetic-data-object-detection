Synthetic Data Object Detection (YOLOv8 + Blender)

This project explores training an object detection model using synthetic data generated in Blender.
Instead of collecting real-world images, a simple 3D scene is rendered multiple times with small variations, and the resulting images are used to train a YOLOv8 model.

Objects Detected

The model is trained to detect three objects:

Table

Bottle

Ball

Each rendered image contains all three objects with randomized position, rotation, and scale.

Project Structure
synthetic-data-object-detection/
│
├── assets/
│   └── predictions/        # Selected prediction samples (manually chosen)
│
├── blender/
│   ├── scripts/            # Blender Python scripts for rendering & labeling
│   └── base_scene.blend    # Base Blender scene
│
├── dataset/
│   ├── images/
│   │   ├── train/
│   │   └── val/
│   ├── labels/
│   │   ├── train/
│   │   └── val/
│   └── data.yaml
│
├── models/
│   └── yolov8n_synthetic_final.pt
│
├── training/
│   └── train.py
│
├── runs/                   # Auto-generated by YOLO (ignored in Git)
├── .gitignore
└── README.md

Synthetic Data Generation

A base Blender scene contains a table, bottle, and ball.

A Python script randomizes object position, rotation, and scale.

Images are rendered automatically.

Bounding boxes are generated directly from Blender in YOLO format.

The dataset is split into training and validation sets.

All training data in this project is fully synthetic.

Model Training

Model: YOLOv8 Nano

Training device: CPU

Epochs: 100

Image size: 640×640

The final trained model is saved as:

models/yolov8n_synthetic_final.pt


Training and prediction outputs are automatically written by YOLO to the runs/ directory, which is excluded from version control.

Results

The model performs very well on the validation set, consistently detecting all three objects.

A small number of prediction images were manually selected and saved in:

assets/predictions/


These images show the final model’s inference results.

Running Inference

Example command to run prediction on validation images:

yolo predict model=models/yolov8n_synthetic_final.pt source=dataset/images/val imgsz=640 device=cpu


Prediction outputs will be saved to:

runs/detect/predict/

Notes

Pretrained YOLO weights (yolov8n.pt) are downloaded automatically and are not included in the repository.

The runs/ directory is generated during training and inference and is ignored in Git.

The focus of this project is the end-to-end pipeline, not real-world deployment.

Purpose

This project was built to understand:

Synthetic dataset generation

Automatic bounding box labeling

Training object detection models with YOLO

Full computer vision pipelines using synthetic data